version: '3'

services:

  geoserver:
    build:
       context: ./geoserver
       args:
        - GEOSERVER_VERSION=${GEOSERVER_VERSION}
        - GEOSERVER_COMMUNITY_VERSION=${GEOSERVER_COMMUNITY_VERSION}
    restart: unless-stopped
    ports:
      - 8080:8080
    environment:
      - SKIP_DEMO_DATA=true

  postgres:
    image: postgis/postgis:${POSTGRES_VERSION}-${POSTGIS_VERSION}
    restart: unless-stopped
    volumes:
      - ./postgres/docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}

  nginx:
    image: nginx
    restart: unless-stopped
    ports:
      - 80:80
    volumes:
      - ./sample_data:/usr/share/nginx/html:ro
